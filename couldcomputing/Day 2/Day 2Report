Hierarchy and structure
Hierarchy and structure
Hierarchy and structure

Managing files:

create a file and directory
delete a file and directory
changing the filename
moving the directory
print the statement in file
copy the file

File Display Content:

display the content
editing the file
display first number of lines
display last number of lines
print the statement in new line
display the file number of lines
